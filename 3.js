var numInteiro = parseInt(prompt("Digite um número inteiro:"));

var numDecimal = parseFloat(prompt("Digite um numero decimal:"));

var resultado = numInteiro / numDecimal;

alert("O resultado da divisão é: " + resultado);
